import { z } from "zod";
import { nullableSchema } from "@/lib/utils";
import { BaseEntitySchema } from "@/services/base/response/BaseResponseSchema";

export const {{pascalCase name}}CreateSchema = z.object({
    name: z.string().min(1, "Nama harus diisi"),
});

export const {{pascalCase name}}UpdateSchema = {{pascalCase name}}CreateSchema.partial();

// Skema untuk form update yang memerlukan ID
export const {{pascalCase name}}SchemaUpdate = {{pascalCase name}}UpdateSchema.extend({
    id: z.union([z.string(), z.number()]),
});

// Skema index untuk response yang meng-extend BaseEntitySchema dan membuat seluruh kolom bisa null
export const {{pascalCase name}}IndexSchema = z.object(nullableSchema({{pascalCase name}}UpdateSchema)).merge(BaseEntitySchema);

export type {{pascalCase name}}CreatePayload = z.infer<typeof {{pascalCase name}}CreateSchema>;
export type {{pascalCase name}}UpdatePayload = z.infer<typeof {{pascalCase name}}UpdateSchema>;
export type {{pascalCase name}}FormUpdatePayload = z.infer<typeof {{pascalCase name}}SchemaUpdate>;
export type {{pascalCase name}}Entity = z.infer<typeof {{pascalCase name}}IndexSchema>;